# Exemple de configuration d'environnement pour PyJAMA / 80_import_postgres
#
# Copiez ce fichier en .env et adaptez les valeurs.

# Mot de passe PostgreSQL utilisé par 80_import_postgres.py
# - lu en priorité via password_env (par défaut POSTGRES_PASSWORD)
# - ou via la variable standard PGPASSWORD
POSTGRES_PASSWORD=changeme

# Exemple d'utilisateur / base pour l'environnement de démo
#PGUSER=pyjama_sample
#PGDATABASE=pyjama_sample
#PGHOST=localhost
#PGPORT=5432

